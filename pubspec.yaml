name: mpk_rzeszow
description: MPK Rzeszów – Live Bus Tracker
publish_to: none
version: 1.0.3+4

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  http: ^1.2.0
  provider: ^6.1.2
  url_launcher: ^6.3.0
  dynamic_color: ^1.7.0
  shared_preferences: ^2.3.0
  package_info_plus: ^8.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
